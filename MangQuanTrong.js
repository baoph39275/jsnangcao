// //3. Các phương thức mảng quan trọng
// //*map() : Trả về mảng mới sau khi biến đổi từng phần tử.
// const arr = [1, 2, 3];
// const newArr = arr.map(x => x * 2); // [2, 4, 6]
// //*filter() : Trả về mảng mới gồm các phần tử thỏa điều kiện
// const arr = [1, 2, 3, 4];
// const even = arr.filter(x => x % 2 === 0); //[2,4]
// //*reduce() :Dùng để tính toán tích luỹ (số, object, array,…).
// const sum = [1, 2, 3].reduce((acc, cur) => acc + cur, 0); // 6
